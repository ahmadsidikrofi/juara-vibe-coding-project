import { Button } from "@/components/ui/button";

const ButtonDefaultDemo = () => {
  return (
    <Button
      className="hover:-translate-y-1 transition-transform duration-200 hover:bg-primary/80 cursor-pointer">Default
          </Button>
  );
};

export default ButtonDefaultDemo;
